<Window x:Class="LangLang.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view="clr-namespace:LangLang.View"
        mc:Ignorable="d"
        Title="LangLang" Height="250" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Main Content -->
        <StackPanel Grid.Row="0" Margin="20" VerticalAlignment="Center">
            <TextBlock Text="Welcome!" Margin="0 0 0 20" HorizontalAlignment="Center"/>
            <Button Content="Login" Command="{Binding NavigateToLoginCommand}" Margin="0 0 0 10" Padding="10" FontSize="14" FontWeight="Bold"/>
            <Button Content="Register" Command="{Binding NavigateToRegisterCommand}" Margin="0 0 0 10" Padding="10" FontSize="14" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
