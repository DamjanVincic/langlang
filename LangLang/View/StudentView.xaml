<Window x:Class="LangLang.View.StudentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:LangLang.View"
        mc:Ignorable="d"
        Title="StudentView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Main Content -->
        <StackPanel Grid.Row="0" Margin="20">
            <Button Content="Courses" Command="{Binding ViewCoursesCommand}" Margin="0 0 0 10"/>
            <Button Content="Exams" Command="{Binding ViewExamsCommand}" Margin="0 0 0 10"/>
            <Button Content="Edit Account" Command="{Binding EditAccountCommand}" Margin="0 0 0 10"/>
            <Button Content="Delete Account" Command="{Binding DeleteAccountCommand}" Margin="0 0 0 10"/>
        </StackPanel>
    </Grid>
</Window>
