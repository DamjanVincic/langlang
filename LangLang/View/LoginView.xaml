<Window x:Class="LangLang.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:LangLang.View"
             mc:Ignorable="d"
             Title="Login" Height="250" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Login" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,20,0,10"/>

        <StackPanel Grid.Row="1" Margin="10">
            <TextBlock Text="Email:" FontWeight="Bold"/>
            <TextBox x:Name="UsernameTextBox" Text="{Binding Email}" Margin="0,5,0,10"/>
            <TextBlock Text="Password:" FontWeight="Bold"/>
            <TextBox x:Name="PasswordBox" Text="{Binding Password}" Margin="0,5,0,10"/>
        </StackPanel>

        <Button Grid.Row="2" Content="Login" HorizontalAlignment="Center" Command="{Binding LoginCommand}" Margin="0,10"/>
    </Grid>
</Window>
